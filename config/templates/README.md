# Shared Instance Templates

日期：2026-03-29

这个目录用于放 Shared 实例配置模板。

当前模板：
- `shared-instance.base.yaml`
- `shared-instance.internal-test.yaml`
- `shared-instance.override.example.yaml`

用途：
- 给 `create-instance` 脚本使用
- 给控制台创建实例时复用
- 避免手拼配置
