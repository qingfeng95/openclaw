# Shared Instance Internal Test Template
# 日期：2026-03-29
# 用途：内测环境模板占位

extends: "./shared-instance.base.yaml"

instance:
  env: "shared-internal"

shared:
  usage:
    enabled: true
  audit:
    enabled: true
